spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/legacy_db
    username: legacy_user
server:
  port: 8080
integration:
  notification:
    endpoint: http://localhost:9090/notify
  file:
    input-dir: C:\\legacy\\inbox
